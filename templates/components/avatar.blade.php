<img src="{{ !empty(Auth::user()->getFirstMediaUrl('avatar', 'avatar')) ? Auth::user()->getFirstMediaUrl('avatar', 'avatar') : Auth::user()->myAvatar ?? asset('images/colissend/default.svg') }}" width="{{ $width }}" height="{{ $height }}" class="rounded-circle" alt="avatar">
